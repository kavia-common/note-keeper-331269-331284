# Backend server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# SQLite database path (provided by the database container in this environment)
# IMPORTANT: do not hardcode; configure in .env
SQLITE_DB=/absolute/or/relative/path/to/notes.sqlite
